/**
 * IIVKIS Portal — Root layout placeholder.
 * Full implementation is deferred to subsequent development steps.
 */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
